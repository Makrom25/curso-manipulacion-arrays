const solution = [1, 3, 5, 7, 10, 11];
const solution2 = [1, 3, 5];

const rta = solution.some(element => element % 2 === 0);
const rta2 = solution2.some(element => element % 2 === 0);

console.log(rta)
console.log(rta2)